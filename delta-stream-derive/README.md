# delta-stream-derive

Derive macro used by [`delta-stream`](https://docs.rs/delta-stream) for application state models.

Most users should depend on `delta-stream` with its default `derive` feature rather than adding this crate directly.
